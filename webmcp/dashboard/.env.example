# ============================================================================
# webmcp/dashboard/.env.example — DB 접속 설정 (대시보드용)
# ============================================================================
# 이 파일을 복사해 .env 로 만든 뒤 실제 값으로 채우세요.
#   cp .env.example .env
# (backend/app.py 와 동일한 DB 접속 정보를 사용합니다)
DB_HOST=192.168.31.136
DB_USER=fortune
DB_PASSWORD=user!1234@abcd
DB_NAME=webmcp
